import { CarretToEndDirective } from './carret-to-end.directive';

describe('CarretToEndDirective', () => {
  it('should create an instance', () => {
    const directive = new CarretToEndDirective();
    expect(directive).toBeTruthy();
  });
});
